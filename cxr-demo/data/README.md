# Training images (local only)

Place the **Chest-XRay-Pneumonia** dataset here after download—see **[`../docs/cxr-demo.md`](../docs/cxr-demo.md)** for the Kaggle source and expected layout (`NORMAL/` and `PNEUMONIA/` subfolders).

This directory is **gitignored** when populated; the public repo only ships the exported `model/` and demo samples.
